version: '1.0.{build}'
before_build:
- cmd: dotnet restore
build:
  publish_nuget: true
  verbosity: minimal
after_build:
- dotnet pack "Serilog.Sinks.Slack\project.json" -c Release
deploy:
- provider: Environment
  name: nuget.org
  on:
    branch: master  
